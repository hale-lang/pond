# hale.toml — link surface for pond/term consumers.
#
# When an Hale program imports this lib, the build picks up
# `csrc` from this file automatically (per spec/ffi.md
# § "hale.toml [ffi] auto-pickup"). libc-only — no `link`
# entries needed.

[ffi]
csrc = ["glue.c"]
